import { IconDefinition } from "@fortawesome/fontawesome-svg-core";

export interface progressProps{
    wrapper?:string;
    color?:string;
    icon?:IconDefinition;
    name?:string;
    wordColor?:string;
}